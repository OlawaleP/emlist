import React from "react";

const HomeSectionTitle = ({ title }: { title: string }) => (
  <h2 className="text-3xl text-gray-900 font-bold max-md:text-xl ">{title}</h2>
);

export default HomeSectionTitle;
